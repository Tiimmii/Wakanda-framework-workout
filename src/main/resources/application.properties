spring.application.name=iam-app
# Database
spring.datasource.url=jdbc:mysql://localhost:3306/iamdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@Tiimmii
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.yaml
# spring.liquibase.enabled=true

spring.liquibase.enabled=false


# # Set default username/password
# spring.security.user.name=admin
# spring.security.user.password=password

spring.config.import=optional:file:./config/application-secrets.properties